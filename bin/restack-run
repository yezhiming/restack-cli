#!/usr/bin/env node

var program = require('commander');

program
  .option('-d, --debug', 'output extra information for debugging')
  .option('-e, --env [env]', 'specify an environment', 'development')
  .parse(process.argv);

require('../lib/server/loader.js');
